version: '3'

services:
  app:
    restart: unless-stopped
    container_name: docuchain-frontend-app
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "3001:3000"
