events {
    worker_connections 1024;
}

http {
    server {
        listen 4000;

        location /app {
            proxy_pass http://docuchain-frontend-app:3000;
        }

        location / {
            proxy_pass https://landing.docuchain.io;
        }
    }
}
